public class Book {
int numPages;
int currentPage;

  public Book (int x){
    numPages = x;
    currentPage = 1;
  }

  public int getNumPages(){
    return numPages;
  }
  
  public void nextPage()
    if(currentPage < numPages){
      currentPage++
       }
    }
}
